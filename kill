local pID = (...)
if tonumber(pID) and process.list[tonumber(pID)] then
	process.list[tonumber(pID)]:resume({"terminate"})
	return process.list[tonumber(pID)]:kill()
else
	proc = process.getFromName(pID)
	if proc then
		proc:resume({"terminate"})
		return proc:kill()
	end
end